# Local Claude-session handoff (переписывается каждую сессию)
NEXT_SESSION.md

# Local project config (Palych/portal settings — machine-specific)
.project.json
.palych.json

# Claude Code local settings (machine-specific permissions)
.claude/settings.local.json

# Build artefacts (Marketplace) — оба регистра, на Linux/CI учитывается
_Market/*.zip
_Market/.last_version.zip
_market/*.zip
_market/.last_version.zip

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml
*.swp

# Logs
*.log
